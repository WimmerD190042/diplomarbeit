import { Routes } from '@angular/router';
import { VerkaufstagComponent } from './verkaufstag/verkaufstag.component';

export const routes: Routes = [
    {path:'Verkaufstag',component:VerkaufstagComponent},

];
